# Flowstate — Build Progress

**Updated:** 2026-08-13 · **Branch:** `develop` (the one dedicated branch — all work happens here; `main` stays frozen until go/no-go)
**Source of truth:** [Charter v3 — DECISIONS LOCKED](.hermes/plans/2026-08-12-charter.md) · **Suite:** ✅ **196 passed / 0 failed / 3 skipped / 8 deselected** (skips = P6/P8 need a relax ≤2 artifact; see P2 note)

## Where to look

| What | Where |
|---|---|
| This progress board | `PROGRESS.md` (repo root) |
| The charter (goal, measurables, decisions) | `.hermes/plans/2026-08-12-charter.md` + `2026-08-12-flow-diagram.html` |
| Per-phase work plans ("dispatch docs") | `.hermes/plans/p1-dispatch1-…`, `p2-dispatch1-…`, `p2-dispatch2a-…` |
| Live data feeds (what the plant is doing now) | GitHub repo **`spartacus242/flowstate-live-data`** (pushed by the file-drop bridge) |
| Daily scorecard runs | `data/scorecards/Week-2026-08-13_*.json` (new file each run) |
| Code history | `git log` on `develop` |

## Phase status (from the charter)

| Phase | What it is | Status | Evidence |
|---|---|---|---|
| **P0** | Sign-off, tidy tree, charter committed | ✅ **DONE** | `6afd246` (charter+diagram), `5297b29` (WW33 baseline + CIP test) |
| **P1** | Live data links (manprg/CIP/stock/AZAP) | ✅ **DONE** | `5117c8d` demand path + staleness; `845dcf6`/`49b904f` GitHub bridge; demand link = `demand_plan_summary.csv`. **Stock check live link landed 2026-08-14:** user pushed the VIF set (ediact 3/4, jestkexp/2, azapart, rmpkitems + receiving xlsm) to the bridge repo; pull conf + .gitignore extended; Stock Check/Reconcile default to `data/reference/`; VIF freshness rule in data_health (26 h). **Three live-format quirks fixed in vif_import** (each silently broke the engine): thousands-comma quantities (8,403/15,267 BOM qtys were NaN → all UNK), mixed date conventions in one export (ediact day-first, jestkexp BBD month-first — per-column auto-detect), headerless azapart (first SKU eaten). Reconcile stock mapping fixed to the real status vocabulary (AT_RISK blocking / TIGHT warn — 'SHORT' never existed). Stock page anchored to the real planning anchor (was showing Feb dates). **Live results:** 70/97 blocks covered, 24 AT_RISK, 3 TIGHT; 24 demand SKUs DO_NOT_SCHEDULE; 1,220 receiving appointments parsed. Suite 232 green. |
| **P2** | Runnable plan core: flat rates, maintenance out, constraint audit | 🟡 **DONE (2 notes)** | `6ecb0bc` flat rates ON; `f3bf013` maintenance out; `d646fd7` probes P1–P9; `e778e6d` min-run floor for current MOs (user-approved, qty-aware). **Notes:** (a) P6/P8 probes need a relax ≤2 artifact — level 0 is hard-INFEASIBLE on this dataset (P11/P13 down + demand) and scenario-E skips levels 1–2; run an A–D solve to exercise them; (b) TRIALS present-but-0-kg oddity open |
| **P3** | Honest scorecard | ✅ **DONE** | `c462769` Service kg real (solver qty_kg → calendar → score, browser-verified 1.8 kg real excess vs fake 0 before); estimated fallback flagged "(estimated)"; trials N/A when no input; CIP gate regression-tested; guard test fixed (was inert). **Open:** Plant Calendar save wipes qty_kg (frontend TS change, P4 scope) |
| **P4** | Daily screens: Reconcile → bidirectional sandbox → lock/export → track | ⬜ PENDING | Sandbox survives; screens not rebuilt yet |
| **P5** | 3-week daily trial with Carsten | ⬜ PENDING | |
| **P6** | Go / no-go | ⬜ PENDING | |

## Gate — the suite right now

✅ **165 passed / 0 failed / 8 deselected** (2026-08-13) — the 5 live-data test failures are fixed: tests now read the pinned snapshot `data/test_fixtures/live_2026-08-13/` (committed baseline), and `tests/test_no_live_data_reads.py` guards against future live-path reads.

## Next up (in order)

1. ~~Stabilize the live-data tests~~ ✅ DONE — suite green (165 passed)
2. **P2 finish:** ✅ dispatch 5 done — min-run floor verified (suite 175 green). **Notes:** P6/P8 coverage needs an A–D solve at relax ≤2 (small follow-up); TRIALS oddity open
3. **P3:** ✅ DONE (`c462769`, suite 196 green, browser-verified). ~~Open: Plant Calendar save wipes qty_kg~~ ✅ fixed (P4 slice 1)
4. **P4:** IN PROGRESS — plan: `.hermes/plans/p4-daily-loop-plan-2026-08-13.md` (4 slices: qty_kg honesty → Reconcile screen → week-lock + mo_changes export → loop framing/weekly roll)
   - **Slice 1 ✅ DONE:** qty_kg survives sandbox edits. Root cause: Python payload legs were already fixed in P3; the real bugs were the frontend mutations — split gave BOTH segments the full kg (double-count), resize kept stale kg. Fixed in `useScheduleState.ts` (split apportions by duration share, segment B takes the exact remainder; resize scales kg proportionally; unknown kg stays unknown; cross-line moves already correct — kg is the invariant, duration re-integrates). Bundle rebuilt. Browser-verified end-to-end on the live app: split 80,000 kg @ 56/111.7h → 40,095.9 + 39,904.1 (sums exact); resize 38.5h→18.5h → 56,800 → 27,300.4 kg (exact proportional). Suite 196 green.
   - **Finding (pre-existing, for slice 3):** saving the calendar persists the display-only cip_info overlay windows ("CIP (sched)") into calendar_blocks.csv — display overlay leaks into the data model on save. Not new; surfaced during slice-1 QA.
   - **Slice 1b ✅ DONE (user request, `e620bc3`):** editable block fields — click a block, type start (datetime) / duration / tonnage; qty↔duration linked via the block's implied rate; block resizes to fit typed kg; same guards as drag (locked/overlap/min-run reject, popover stays open to correct). Adaptive resize-handle widths (5–14px). Browser-verified: typed 40,000 kg auto-resized 111.7h→55.9h and saved exactly; 2h rejected with clear message. Suite 196 green.
   - **Slice 3 ✅ DONE (`c434021`):** 2-week lock window (lock line + shaded zone on the Gantt; drag/resize/edit refused inside; nothing movable INTO the frozen zone; exact h336 boundary verified), resize-handles-ignore-locks bug fixed, saves strip display-only cip_info overlays (was writing phantom CIPs), Lock & Export section on the calendar, Plant write-back section on Compare (mo_changes.csv review + CSV download, live: 40/40 MOs, −247,305 kg delta). Suite 226 green.
   - **Slice 4 ✅ DONE:** nav restructured to walk the daily loop (Home · 1 Connect · 2 Reconcile · 3 Plan · 4 Lock & Export · 5 Track · Setup) per user request "UI should follow our process"; weekly roll advances the lock with the anchor (one action).
   - **Slice 4b ✅ DONE (user-approved "all 3"):** page decluttering. Home: 7-stage SVG pipeline (predated Reconcile) replaced by a live 5-step "Today" walk of the loop (Connect feed freshness · Reconcile blocking/attention counts from the engine · Plan block count + anchor state · Lock state · Track scorecard week), data-status table collapsed. Plant Calendar: one "Start of day" strip (downtime editor + view options + reload), duplicate STEP-1 expander removed, Now-running collapsed, version-name field moved beside its Save button — the Gantt is the page. Data Files → "Connect — Data Files": downtime editor removed (lives on the Calendar), per-file in-browser cell-editing grid dropped (Excel + re-upload; every write still backed up), upload/preview/download kept. Suite 226 green.
   - **Slice 2 ✅ DONE:** Reconcile engine + screen. `helpers/reconcile_engine.py` (pure, 23 tests): STOCK (from stock_check_report; DO_NOT_SCHEDULE→blocking), COVERAGE (zero-scheduled due week 1→blocking; under-min→one summary; unknown-kg honesty note), CIP (overdue-now→blocking; due ≤48h unscheduled→warn; silent when a CIP is planned), CAPABILITY (scheduled-incapable→blocking; manprg-vs-table→warn, table is what needs fixing), FIT (overlaps/production-over-downtime→blocking; past-horizon→warn). `pages/reconcile.py` renders ranked findings with deep links; nav group added. **Browser-verified on live data — real catches:** 14 blocks scheduled on downed P11/P13, P12/P15/P22 past CIP interval, 280480-W0 (355.5 t) due week 1 with nothing scheduled, 241283@P22 missing from capability table. Suite 219 green. The engine doubles as the future planning agent's situational input (one engine, two consumers).
5. **Agent dry-run 1 ✅ RAN 2026-08-14** (Reconcile findings → Scenario E → reasoned sandbox `agent_proposal_2026_08_14`, official untouched). **Agent's own verdict: DO NOT PROMOTE — diagnostic value only.** It proved the loop end-to-end and exposed the two real blockers for agent-grade proposals: (a) the relax ladder lands at level 3 (**ignore_co**) on this dataset, so proposals are changeover-blind (+82 COs) — the known level-0-infeasibility fix (producible-zeroing) is now the top solver priority; (b) stock-blindness at scale — the solver poured **1,093 t across 49 blocks into component-blocked (DNS) SKUs**; next lever = agent deprioritizes DNS SKUs in the work-dir demand copy pre-solve (policy needs user sign-off) or a component constraint in the model. Also noted: composite compares unequal scopes (97-block 2-week board vs 201-block 3-week plan) — cross-horizon score honesty worth a look. Upside proven: 95.9% utilization, 1,076 t of 280480 covered (the fixable week-1 hole), zero downed-line conflicts, CIPs inside intervals.
6. **Solver un-blinding ✅ DONE 2026-08-14 (`6a007a9`, `11f971a`):** bisection cornered THREE presence-gating bugs in the changeover block (first-flag and pairwise ordering compared against absent orders' collapsed-to-0 intervals; week-gap stitch classified committed MOs) — invisible until live gates arrived. Fixed + producible-zeroing v2 (provable window-capacity clamp, 0 fires on this dataset) + ladder skip {0:3} removed + warm-start hints gated by previous relax level (and the runner now carries prev_feasibility.json across its wipe so the gate actually fires). **Dry-run 5: ladder landed at level 1 — changeovers ENFORCED, dues HARD, proposal −6 changeovers vs official (first negative ever).** P6/P8 probes now exercise (3 skips left, rate-basis only). Known remainder: level-0 needs the TRIALS-pseudo-MO-as-blocked-time fix (manprg TRIALS rows are prod≡0 vs qty_min>0 by construction); cross-horizon composite comparison (97-block board vs 504h plan) still apples-to-oranges.
7. **UI batch ✅ DONE 2026-08-14 (user list, `61347a6`, `6206cdf`, `f68a8f7`, `11f971a`):** PDF import + manual schedule upload removed (manprg IS the schedule; DnD IS manual); HSM* = semi-finished (ediact 4 recipes now feed the BOM graph — explode through to sub-components; live: 4 blocks + 4 DNS SKUs unblocked); highlight dims other blocks to 25%; popup fields widened; snap-left/right respecting setup_hrs (CIP absorbs); sub-setup placement warns orange, never silently; insert-between with slide-preview + guards (locked/horizon reject, overlays exempt).
8. **Scenario F ✅ BUILT + LIVE-VERIFIED 2026-08-14** (`924bae0`, `c00021d`; design `scenario-f-fill-the-tail-2026-08-14.md`): the user's process as the daily driver — manprg+cip_info laid out as FIXED line-time (coalesced blocked windows; committed plan never rewritten, no current_mo/mo_changes), CIPs projected at real per-line intervals through the horizon (solver CIP generation stood down), demand reduced by committed production (surplus carries forward only), fill gated at each line's committed WORK tail, idle_weight=3 packs tails, DNS trim still applies. agent_propose defaults to F (E kept as diagnostic). **F run 3 (live): LEVEL 1 — dues hard, changeovers enforced — 92 blocks, +9 changeovers vs official across 3 weeks (E's best: +67), −16 late orders, +4.4h avg run, CIP par, DNS 7 capped blocks/261t.** Also: completed MOs hidden again (user revision; classifier + counts kept), warm-start gated by input signature (stale hints from changed inputs starve the search — measured).
9. **Demand-vs-committed netting ✅ BUILT + DRY-RUN 2026-08-16** (user request from the W34 calendar screenshot: "280480 is scheduled on 4 lines for W34 — don't re-plan it"): new SKU×week coverage ledger `helpers/demand_coverage.py` — ONE netting engine for F staging, Reconcile, the Plan-page preview, and the holding cards. **Convention locked (validated live): the demand plan is GROSS** (280480: 731 t committed vs 738 t cumulative W34–W36 — the plant pre-builds future weeks); committed kg lands in the ISO week of the block midpoint, surplus carries FORWARD only. **Two real bugs fixed:** (a) week_index keying silently no-opped ALL netting when the demand file anchored at an older ISO week than the staging frame (live: W32 file + rolling anchor → keys off by two) — ledger keys by TRUE ISO week; (b) the Thursday trap — completed MOs are dropped by current_state, so kg made Mon–Wed vanished from mid-week netting; completed rows now credit their **made kg** into the week produced, with reference-file history weeks as sinks so past pre-builds never double-credit surviving weeks. Murky cases are FLAGGED never guessed (OVERCOMMITTED: surplus no demand week absorbs — live: 280451 +31 t, 280586 +15 t, 120436 +14 t, 280453 +14 t; plus committed SKUs absent from the plan). Holding cards credit covered kg (the circled 280480-W34 card no longer sits in holding). **Dry run (agent_propose F, live data): netting fired on 83 orders, 1,464,505 kg of already-made production credited, 280480-W34/W35 targets → 0, solver's only new 280480 blocks are the W36 residual (~104 t on P19/P21) — and the ladder landed at LEVEL 0 for the first time** (level 0 was hard-INFEASIBLE pre-netting; shrunken targets made it feasible). Thursday simulation (`scripts/diag_demand_coverage.py --now "2026-08-20 12:00"`): 340 t of 280480 made Mon–Wed still nets W34 to 0 mid-week. Suite 278 green (+21 ledger tests). **Note:** dry-run composite 58.4 vs official 63.5 (+82 TTP changeovers at level 0) — solve-quality tuning at level 0 is follow-up work, not a netting issue.
10. **E2E walkthrough ✅ PASSED 2026-08-18/19** (user: "run the program yourself top to bottom… verify all numbers yourself"): full loop driven in-browser — F run → promote → fill-right (tonnage scales by the block's own rate, exact) → 2 pins → re-solve held pins byte-identical (staged as committed blocked windows) → lock through Sep 1 → Excel export + mo_changes write-back. 12 findings, ALL fixed + merged: Home/Reconcile coherence (shared stock resolver, same assess data), st.dataframe-in-collapsed-expander trap (st.table), past-ISO-week demand demoted to INFO, CIP horizon clip, orphan version slots surfaced/counted/deletable (+ Windows read-only rmtree), mid-solve reattach via run_pending.json, scorecard-history OK entry, capped weekly fulfillment credit (min(assigned, target) in BOTH weekly_breakdown and kpi.ts), adherence past-week filter, block-implied popup rate. Suite 311 green at close.
11. **Planner batch ✅ BUILT + REVIEWED 2026-08-19** (user list): CIP count reported-only (was never scored — pinned by test); scenario versions save NEW timestamped honest names ("Scenario: Pass 1 - Seed & Fill, Pass 2 - CO Optimized 26-08-19 10:36") with oldest-auto-first slot eviction (user-named/renamed/annotated NEVER evicted); Compare reruns 30-40s → ~0.5s (st.cache_data keyed by version mtimes × scoring_inputs_signature); nav group 4 renamed Compare & Promote (Lock & Export lives on the calendar, now WITH an Export button); live-score fingerprint cache on the calendar; **solver rulebook** (helpers/solver_rules.py: 23 knobs + 34 fixed rules rendered on Generate) after tracing EVERY custom weight end-to-end — 2 real defects found+fixed (min_run_pct_of_qty was dead config nothing read; greedy seed priced transitions without flavor_weight/0-clamp), 2 help texts corrected, 3 hard-rule knobs added (min_run_hours / min_run_pct_of_qty / max_lines_per_order, wired UI→toml→P, real-solve smoke-verified); **blank-space SKU picker** (right-click a gap: capable demand SKUs sorted by open kg, per-machine CO chips incl. conv-to-org/cin-to-non from changeovers.csv bitmask payload ≈136KB, snap-left with setup, per-order segment split, holding recalcs on Refresh — fixed a pre-existing gap where rebuilt holding was never re-adopted without remount). 21-agent adversarial review of the merged diff: 16 confirmed findings ALL fixed (stale-cache keys incl. line_rates.csv in SCORING_INPUT_FILES, curation-protects-eviction, off-grid placement rounding, frozen-overlay right-click, read-only mounts, unknown-pair "?" chips, home.py step-4 label). Suite 371 green. **Open question for tuning:** soft-demand over-target reward is effectively ~0.005%/kg (comment claims 5%) — flagged in the rulebook, behavior unchanged.
12. **Supply Timeline ✅ BUILT + VERIFIED 2026-09-01 (uncommitted in the working tree at close):** open-PO receipts + a time-phased component timeline on the Plant Calendar (plan `.hermes/plans/2026-09-01-po-stock-gantt-plan.md`, contracts `…-po-stock-contracts.md`). The stock engine was a flat snapshot (every block vs the full on-hand pile); `stockcheck/timeline.py` now nets consumption in board order and steps receipts in on their ready hour, grading each block OK / OK·backed / DELIVERY-DEPENDENT / SHORT / NO_DATA against a 4-day buffer measured to block start (`[stock]` in flowstate.toml). Feed: `data/reference/open_pos.xlsx` via the bridge (`stockcheck/po_import.py`; item code = BOM code, `Receipt date` authoritative, overdue lines never counted, landed rule by N-batch date, raw areas +72 h QC, SL3 needs a transfer appointment; dock sheet joins on the 8-digit PO suffix). Surfaces: Gantt chip/hatch/tick on the block face, drag-row + post-commit banner (warn-only; hard_block=false), popover Supply section with Acknowledge, header supply pill, holding-card / picker / place-row safe-start pills, one 🚚 per receipt day on the axis, `?focus=<block_id>` deep link; Stock Check Inbound tab + Supply column; Reconcile `supply:` findings (SHORT blocking on a fresh feed); calendar caption; data_health rows `open_pos`/`receiving`. TS port `utils/stockRisk.ts` parity-tested against `data/test_fixtures/stock_risk/cases.json`. Split-MO pieces (shared `block_id`) now key by `id|start_hour` everywhere (pre-existing by-id mutation bug fixed). **Popover refined 2026-09-02 (planner feedback):** the Supply section lists only depleting items (SHORT / DEPENDENT / NO DATA / backed), OK items stay behind the collapsed toggle, untracked items and the per-item "also drawing" lists are gone; a **Supply details** button opens an in-Gantt panel per at-risk item with the blocks drawing it (window, draw qty, running balance) and the open POs (qty, receipt date, ready stamp, lead) — `components/SupplyDetailPanel.tsx`, `supplyGlue.supplyDetail`, tests/test_supply_detail.py. Browser-verified headless on a synthetic future-dated PO file (7 short · 2 dependent, chips/hatches/trucks/pills/acknowledge all live). Suite 830 green (was 611). **Slice 3 (solver DNS trim per order + earliest-start constraint) deliberately NOT built — separate sign-off.** Live caveat: the 8/24 sample IT sent is content-stale (all dates before today) so every gap reads NO DATA until IT delivers a ≥5-week extract with an as-of stamp — the ask list is in plan §3.
13. **ERP PO export `order_npa.csv` ✅ BUILT + LIVE-VERIFIED 2026-09-14** (user: "this is how the ERP will export it from now on … it will take the place of the open_po file"): the ERP (Sage X3) now exports its own PO lines — pipe-delimited, Windows-1252, 219 French-labelled columns, 13-digit fixed-point numbers with four implied decimals (`0000180000000` = 18000 kg, pinned against the old workbook on three POs), DD/MM/YYYY dates, **no quoting** (a literal inch mark and commas in city names are why Excel mangles it). `stockcheck/x3_po_export.py` reads it in full (every column decoded + the raw strings kept; repeated French labels mapped by occurrence; header drift accepted-by-position / kept as `extra_<n>` / listed as missing; wrong-count rows kept and flagged) and projects it onto the PoLine contract (`qty` = **remaining** qty, `received` = nothing left to receive; the ERP's `line_status` rides along uninterpreted — 70 is ambiguous, ask IT); `load_open_pos` detects the layout, the legacy workbook path is untouched. Resolver order `order_npa.csv` → `open_pos.xlsx` → `open_pos.csv`; bridge conf lists the file (git-ignored); Inbound tab shows `ordered` + `status`; `scripts/convert_order_npa.py` writes an Excel-clean workbook (+ *Columns* sheet). Live run on the first export: 983 lines / 700 POs, feed OK, 512 lines counted as receipts on 162 items, 299 SL3 lines held for a transfer appointment, 0 import errors. Structure reference: `docs/order_npa_export.md`. Parity test: the same four PO facts through the xlsx and the export give byte-identical receipts/joins/verdicts.
14. **Supply Timeline slice 3 — solver stock policy ✅ BUILT 2026-09-15** (user: "build slice 3"; plan §6 row 3 / decision 10, contract §10 in `.hermes/plans/2026-09-01-po-stock-contracts.md`). Until now the CP-SAT solver never saw the PO feed: the only material input was the 2026-08-14 flat DNS trim (worst-week ratio vs the whole on-hand pile, no board netting, no inbound, no timing). New pure engine `stockcheck/projection.py` grades every demand order on the SAME per-item curves the Gantt uses: the board's committed blocks draw first and credit their week (`board_kg`, overlap pro-rated), orders are netted in (week, priority, largest-first) order with a per-source ledger (on-hand vs each receipt), gated PO receipts lift what on-hand cannot support, and every counted receipt pins the order's EARLIEST START at ready + `[stock] min_days_after_delivery`. A truck is only worth waiting for when `min(1, ratio) × (share of the week left after ready + L)` beats the on-hand share; receipts never touch an order on-hand already supports ≥ 90 %. Report: `demand_view[i].projected` (status COVERED / OK / LIFTED / DNS / NO_DATA, `cap_kg`, `earliest_start_h`, per-group balance sheet, sentence) + top-level `projection` counts. Policy: `helpers/agent_policy.apply_stock_policy(work, report)` is the ONE patch the Generate page, `agent_propose.py` and `overnight_batch.py` run (netted rows `qty_max = min(qty_max, cap_kg)`, pct rows on `(board + cap)/target`, `qty_min → 0`, floors written to `earliest_start_hour` in the WORK frame via the report-anchor → staging-anchor shift; flat trim stays as the fallback for an old cache; `[stock] solver_policy = projected|flat|off`, `solver_earliest_start`). Solver: `data_loader` reads the column, `model_builder.effective_due_start` raises the floor under every early-fill policy (dead pairs, window-capacity clamp and the interval constraint all see it; cross-week adds the hard floor), `greedy_fill` clamps its window, `independent_validator` check `STOCK_FLOOR`. Surfaces: Stock Check → Demand tab `Solver / Projected / Cap kg / Earliest start / Why` + policy caption; Reconcile `stock_dns_demand` names capped orders (lifted ones apart). **Live dry run 2026-09-15 (board anchor 09-14, feed OK):** 238 orders → 28 OK · 16 covered by the board · 78 lifted by POs · 116 capped; 95 floors (W0 none, W1 24, W2 17). Inside the solver's 3 weeks: of the 18 flat-DNS orders 11 are lifted by trucks; of the 80 flat-OK orders 14 are now capped (the board or an earlier week already took the component — the flat view let every order claim the full pile) and 24 wait for a truck. Suite 1376 green (+63 new: `test_projection`, `test_agent_policy`, `test_stock_policy_solver`, `test_stockcheck_projection_report`); the known-permanent `test_live_scenario_f_snapshot_runs` still deselected. Uncommitted in the develop working tree together with the shared-folder feed work (item 13's follow-up).
15. **Week score cards: trustworthy + a week filter ✅ BUILT 2026-09-15** (planner: "the score cards above the calendar should be trustworthy … especially for W0 … when MOs have been completed or are currently running", plus "a selector for which weeks will show the scorecard … and in the holding area"). **The selector:** `st.multiselect` above the board (`cal_weeks_shown` → `config.weeks_shown` → `visibleWeekCards` memo + `HoldingArea.visibleWeeks`) hides a week's score card AND its holding column. **Render-only by construction** — `computeAdherence` / `computeKpis` / `countChangeovers` inputs, `deriveAutoHolding`, the `holdingArea` state, undo/redo snapshots, `setComponentValue`, the Python holding rebuild, Save and the export all still see every week, so hiding a week moves no number and loses no card (the holding header reports "N hidden by the week filter"). Verified live: deselecting WW38 took the card count 7→6 and its holding column away, header "165 cards · 2 hidden", every other card byte-identical. **Four card-trust defects found by forensic reproduction on live data and fixed:** (a) a running MO's board block freezes remaining kg + a `made_kg=` token at rebuild while the credit was rebuilt from LIVE manprg every render, so the sum drifted past the MO's Fct (an order read OVER) and an MO finished since the rebuild lost its made share entirely (an order read ~10% with a phantom holding card) — `demand_coverage.reconcile_completed_to_board` now credits the FROZEN token, making board kg + credit == Fct in both drift directions; (b) Sunday production keys to the previous ISO week and was discarded as `unsettled_past` (139,255 kg live, five W0 orders reading 0% with phantom cards) — `build_ledger_from_data` carries it forward on the **made_only path only** (the solver keeps refusing to guess, fix C54), bounded to the week immediately before the plan; (c) "hide blocks that already finished" could hide ONE piece of a still-running split MO, deleting its kg from both the board and the credit — `calendar_io.split_finished_rows` hides an MO whole or not at all; (d) the cards' "+ N% already made" was partly BOARD kg the waterfall displaced into a neighbouring week (live: all of W40's) — `kpi.weekCardShares` reads the two shares directly. Ledger WARNING/NOTE diagnostics, previously dropped on the floor, now render above the board. **Live effect:** W0 credited orders 11→16 and 569,325→682,385 kg; W0 holding 8 cards→2. **Adversarial review (56 agents, 4 lenses × 2 skeptics): 26 findings, 12 confirmed, 4 of them regressions in this change, all fixed** — the frozen credit was multiplied when one MO runs on two lines (now keyed per `<mo>@<line>` with a fail-safe that under-credits rather than double-credits), every CIP block collided under one blank `order_id` so a later clean kept all finished cleans visible (55 CIP rows, 17 wrongly kept at h120), the week-choice past filter compared bare ISO week numbers, and a re-imported demand plan neither re-based nor auto-showed the selection. **Known follow-ups (confirmed, deliberately deferred):** the `made_kg=` token is rounded to whole kg so the identity is exact only to ±0.5 kg/MO (`current_state.py` `:.0f`); a running MO whose board block has entirely elapsed still loses its remaining kg from both sides under hide-past; and the cards (board-only pass) can disagree with the Python adherence table / holding (credited pass) — the root fix is applying `covered_by_order` AFTER the block passes in `_credit_blocks_to_orders` and its `kpi.ts` port, which moves the parity goldens and belongs in its own change. Tests: `test_card_credit_trust.py` (credit rules, carry, split/CIP hiding), `test_week_card_shares.py` (tsc→node). Suite **1470 green**. Uncommitted on develop alongside items 13/14.
16. **Shared-folder live feed ✅ BUILT 2026-09-14** (user: the ERP already drops its exports into a folder on the work share; the planner's PC should read that, not GitHub): `scripts/fs-live-pull.py` gained **folder mode** — `source_dirs` in the git-ignored `fs-live-data.local.json` (or `--source-dir`) and the pass copies straight from the folder(s) into `data/reference`, same file list, same dated-name globbing, same `demand_plan.csv` derive, manprg as-of = the ERP's write time. Read-only towards the folder (no lock, marker, rename, delete). Hardened for a shared drive: a file modified < `settle_seconds` (60) ago or changed while being read waits a pass; one busy file costs only itself (a sharing violation used to abort the whole pass); copies swap in atomically with the source mtime kept; every pass — failed ones too — writes the `live_sync.json` heartbeat and exits 1 on problems (the installer's first-sync warning was dead code before: `main()` swallowed every error). `install_flowstate.ps1 -FeedDir "\\server\share\erp_out"` writes the override, runs the first sync, registers the 5-min task (GitHub mode via `-LiveData` unchanged, 30 min); no GitHub login on the planner's PC — the code repo is public. App side: `helpers/live_sync.py` (runs the pass as a bounded subprocess, reads the heartbeat), Home's new **Live data sync** health row (NOT_APPLICABLE when never run here; STALE when silent > 1 h or problems; never blocking), and both **Rebuild from plant state** buttons sync first and rebuild from the fresh files (`cal_rebuild_pending`). `fs-live-push.py` reads `source_dir_work` from the conf so the work PC can watch the same share. Docs: README install section, `docs/live-data-bridge-setup.md` PART 6. Tests: `tests/test_live_folder_sync.py` (17).
17. **IT answers on order_npa.csv ✅ 2026-09-15** (meeting notes from the user): line status 20 = receivable, 60 = archived, 70 = deleted → `to_po_lines` now marks 70 lines `cancelled` (own timeline fate, never inbound — the 45 full-qty status-70 lines used to count as receipts under the remaining-qty rule), 60 lines closed, `status_text` on every line (Inbound tab *state* column); KEA = thousand each → KEA lines scaled to EA (`unit_original`), PO 30044091 no longer a unit mismatch; export leaves the ERP ~19:10 daily and lands in the SharePoint library **VIF Extracts** (NPA_ContinuousImprovement) within 15 min, same name, no archive — the work PC's OneDrive-synced path is now `source_dir_work` in the bridge conf, and a planner's PC syncs the same library for `-FeedDir` (docs: `docs/order_npa_export.md` *Answers from IT*, bridge doc PART 6, README). Tests +2.
18. **New data sources ✅ 2026-09-15/16** (the plant's first full drop, `C:\Users\<user>\Flowstate\fs_data`; reference `docs/vif_exports.md`): two folders — `fs_vif` (the ERP's VIF / Sage X3 exports) and `fs_manual` (people-maintained: `cip_info.csv`, the dock schedule, the weekly `New Export AZAP MMDDYY.xlsx`, `demand_plan_summary.csv`; `changeovers.csv` to come). **New files:** `ediact.csv` (headerless BOM, no flow-type column, 24,534 rows of which 8,443 exact duplicate lines are dropped) — it does **NOT** carry the semi-finished HSM/RF recipes: `ediact 4.csv` stays the recipe supplement, and without it the loader notes "semi-finished recipes missing: 17" (`snap.missing_recipes`, report `missing_recipes`, a Stock Check banner, a STALE VIF health row) while those intermediates read NOT_TRACKED (their ingredients are then never checked); `jestkamb.csv` (Americold Burley AMB: 1,864 lots, 242 t Ava / 1,497 t Loc), `jestkexq.csv` (QC depots QCR/QCP), `jestksav.csv` (fresh apples SA1/SA2, 899 t, not a recipe input), `jestkexp5.csv` (semi-finished lots, pipe-delimited; shown, never counted — 2026-08-14 rule), `PKG-REC.csv` (packaging receipt slips, 390 rows under a preamble + header). **De-dup:** `jestkexp4.csv` = byte copy of `jestkexp.csv` and `jestksa.csv` = `jestksav.csv` minus four columns (fallback aliases only), `jestkex2.csv` = stale 2025 packaging snapshot (ignored), `jestkami/jestkavg/jestktgb` empty exports, 9 QCR lots listed in both `jestkexq` and `jestkexp5` kept once (first file wins). Nothing in `fs_data` is deleted: the ERP rewrites those files every run, so the loader ignores them instead. **Loaders** (`stockcheck/vif_import.py`): BOM from the first of `ediact.csv` / `ediact 3.csv` that loads with rows (an empty or unreadable `ediact.csv` falls back), frame key stays `"ediact 3.csv"`; every lot file mapped by header name into one shape + `source`; `lot_frames` / `receipts_frame`; required exports (BOM, `jestkexp`, `jestkexp2`, `azapart`) missing = an error again; lot BBDs month-first. **Engine:** toggles — AMB, SA1, SA2, M12 `Ava` on; QCR/QCP off; `Loc`/`Out`/`OL` off; grouped depot rows on Stock Check. Gate snapshot date = the raw/packaging exports only (a stale side file can no longer loosen the overdue rule). Receipt slips land a PO line only when booked before the stock-export day (or their batch is already in the lots) and then consume the matching lots; a slip booked on/after the export day counts the truck as a receipt on the slip date; slips are checked before the overdue rule. Apple lots count on hand but land only same-day lines of the snapshot day. A semi-finished requirement without its recipe reads NOT_TRACKED, alternates included. `bom.py`: an exact `sku` activity is the only entry point when it exists (`ediact.csv` files `280351-DS` under PF 280351, which had doubled all 24 of its requirements). **AZAP parser** (`helpers/azap_demand.py`, `scripts/azap_demand_summary.py`): `Factory == NPA`, no `To Export` filter, `Trial` dropped, tons summed per ISO week × product, 7 weeks from the first Monday after the file-name date, byte-format `Week,Product,Tons` — all 238 current rows reproduce; newest export by file-name date (a passed-over workbook saved later is reported every pass); rebuilt only when the csv is not the build of that workbook; refuses a 0-row or under-half result; no stray `.tmp` in the shared folder; the sync (folder mode) and the push script rebuild it, the CLI defaults to the configured source folders. `demand_summary_import` resolves the ISO year once per file (New-Year windows). **Verified on the real drop:** old files vs new drop + `ediact 4.csv` grade all 166 blocks identically; 280397-W3/W4 move DNS → OK on 12.8 t of AMB cherries (750158); 12 PO lines land by slip; full suite 1,515 passed.
19. **Data Files = every input, status first ✅ BUILT 2026-09-17** (user: "update the Data Files tab to incorporate all data files used by the system … I want Carsten to be able to look at that screen and quickly identify if anything is missing or stale"; and "remove the reconcile page"). The page had listed the 11 planner CSVs only — manprg, the whole 2026-09-15 VIF drop, `order_npa.csv`, the receiving workbook, the demand baseline and the measured-rate tables were invisible. **Registry:** `helpers/data_catalog.py` now names all 30 inputs with group, kind (csv / plant text / workbook / toml), source, expected-refresh key (the Command Center's `DEFAULT_CADENCE_H`), optional flag, fallback names (BOM `ediact.csv` → `ediact 3.csv`, PO `order_npa.csv` → `open_pos.xlsx` / `.csv`, lot aliases) and the `[datasources]` override key; VIF rows resolve against the folder the Stock Check actually reads (`stock_report_inputs`). **Engine:** `data_health.file_statuses` grades each file present → readable → fresh on the file in use (manprg by its as-of stamp), optional-absent reads grey, fallbacks and configured paths are named in the note; the generic Home loop skips rows a dedicated feed rule already judges (`generic_health=False`) and absent optional files. **Page:** live-sync heartbeat + **Sync now**, four count chips, one sortable table of every file worst-first (status · file · what · group · updated · age · expected refresh · rows · source · note), then the capability check, the demand import (now naming the upstream AZAP workbook in folder mode) and one expander per file grouped by origin (plant state, demand, component stock, inbound supply, master data, measured history) with preview (`st.table` for csv, raw head for plant text, size for workbooks), download ≤ 1 MB, upload for planner CSVs. **Reconcile page removed:** `pages/reconcile.py` deleted; the engine stays (Home step 2 keeps its counts and opens Stock Check; the past-weeks finding lost its deep link). Drift guard: `tests/test_data_files_catalog.py` fails when a bridge-delivered or loader-known file has no row. Tests +15; smoke tests for the page.
20. **fs_data root + PO buyer comments ✅ BUILT 2026-09-17** (user: "when we install on one of our work computers it should pull from a sync'd SharePoint folder with the structure fs_data -> fs_manual & fs_vif"; and "on the order_npa.csv file there are comments added by our inventory specialists. Can those be displayed in the various places we view open PO's?"). **How the dev PC gets its data today (verified):** the hand-made task `\flowstate-live-pull` runs `scripts/fs-live-pull.py --once` every 30 min in GitHub mode (clone `C:\Users\jbdil\FlowstateLive` of the private live-data repo) into `data/reference`; the work PC's push has carried only `cip_info.csv` since 2026-09-15 (last manprg 09-15 11:38, order_npa 09-14 14:28) and none of the 09-15 drop files — its conf/folder still predates the fs_data layout. **Folder mode accepts the drop ROOT:** `expand_drop_roots` (verbatim in `fs-live-pull.py`, `fs-live-push.py`, `azap_demand_summary.py` and `helpers/live_sync.py`, pinned identical by `test_live_bridge_glob`) replaces a configured folder holding `fs_vif` / `fs_manual` by those subfolders (fs_vif first; the root itself is dropped so a workbook left there never gets the summary rebuild; explicit lists and flat folders unchanged); a reachable folder holding none of the conf files is now a problem (`no plant files found in …`, exit 1, Home row STALE) instead of a silently green "nothing new"; the installer names what it found in each `-FeedDir` folder and recommends `-FeedDir "<path>\fs_data"`; docs updated. Proven end to end on an isolated copy of the local drop: 19 files landed incl. PKG-REC / jestkamb / jestkexq / jestksav / jestkexp5, summary rebuilt from the AZAP workbook, heartbeat names both subfolders. **PO comments:** `to_po_lines` already decoded `comment` (internal, 216 of 983 live lines / 72 POs) and `comment_external` (99 lines), 50 chars each, per LINE; they now ride (additively, `""` when absent, never inside the pinned `label` / verdict sentence) on receipts, bindings, counted receipts and the Gantt payload, and show on: Stock Check Inbound tab (*Buyer note* / *Supplier note* columns), Board tab caption under a block's Supply line, the Excel export's new **Inbound POs** sheet (string cells kept text — a note starting with `=` is not a formula), the Gantt popover receipt rows, the Supply details panel *Note* column, the header-truck tooltips, and the reconcile finding detail. Frontend rebuilt (dist committed), `stock_risk` fixture regenerated, contract doc + `docs/order_npa_export.md` updated. Built by two parallel implementers, 4-lens adversarial review (5 low/medium findings fixed, 2 refuted). Suite 1687 green + the known-permanent validator test. Tests +40.
21. **P5/P6:** trial with Carsten
