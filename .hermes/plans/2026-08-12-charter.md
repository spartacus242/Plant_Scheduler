# Flowstate Charter — v3 (plain language) — DRAFT for review

**Date:** 2026-08-12 · **Owner:** jbdil · **Stakeholder:** Carsten (production planner) · **Status:** DECISIONS LOCKED — D1–D5 answered 2026-08-12 (§10)
**Execution model:** Director plans, audits, and verifies on its current cost-effective model. **Claude Code does ALL coding changes** — default coding model: **Opus 5** (same cost as 4.8); simple tasks go to cheaper models (haiku/sonnet) or the Director directly. Resources are allocated cheapest-capable — never Opus for trivial work. No code moves without a passing Director check.
**Branch:** `feature/handoff` is the working trunk (`main` is stale — never branch from it). ~150 tests.

---

## 1. What changed from v2 (your feedback, folded in)

- **Demand/capacity corrected.** Verified against `demand_plan_summary.csv`: W33 1687 t, W34 1840 t, W35 1766 t (W32 ≈ 1954 t from the live export). Capacity with the flat rates you uploaded: 14 lines × rates = 14,702 kg/h → **≈ 2,470 t/week max** (≈ 2,300 t realistic after cleaning and changeovers). **Demand fits.** The old "half the work can't fit" claim was a mis-read (a 4-week total compared against a short window) — retracted.
- **Main goal: produce the demand plan tonnage in the most efficient way — minimal changeovers, maximum throughput.**
- **Flat per-line rates** replace per-SKU rates. Good news: the solver already supports this — `use_sku_rates = false` + a `line_rates.csv` (`line_id,Line,rate_kgph`, exactly your file's schema) is read by `data_loader.py:169-187`. Small ripple: the data-health check and the scorecard's kg lookups must follow.
- **Maintenance:** still blockable on the calendar (block the time, solver works around it), but **removed from the scorecard entirely.**
- **Sandbox is two-way:** solver proposes → you drag & drop; you pin blocks → solver replans around your pins.
- **Stock check is part of Reconcile:** every SKU on the plan is checked against component stock before it's locked.
- **All coding by Claude Code.** The Director never edits code; it writes specs, audits, runs the verification gates, and reviews.

## 2. Goal (plain language)

> **Every day, Flowstate turns the plant's live data into ONE plan that makes all of the demand tonnage, on time, with the fewest changeovers and the highest throughput — 2 weeks locked and ready, week 3 flexible. Stock, cleaning, and capacity problems are flagged before you plan; you adjust with drag & drop; the tool replans around your blocks; changes go back to the plant system; the scorecard is honest.**

## 3. The daily loop (unchanged from v2)

**Connect** (live data: manprg, CIP, stock, AZAP) → **Reconcile** (stock flags, orders that won't fit, cleaning due, capability conflicts) → **Plan** (solver proposes ↔ you drag & drop, both directions) → **Lock & Export** (2 weeks locked, changes written back to VIF) → **Track** (honest scorecard, no maintenance) → repeat tomorrow. **Each week the lock rolls forward one week.**

## 4. Measurables — what success looks like

| # | What it means | Today | Target |
|---|---|---|---|
| **M1** | **A plan that fits.** Runnable as-is, on real time. | Fits in principle (demand < capacity) | 100% of daily plans fit real time; committed MOs keep ≥85% of their amount |
| **M2** | **Demand covered.** All demand tonnage makes it into the plan. | No coverage report | 100% of demand tonnage scheduled (or on the waiting list with a reason); sums add up ±0.1% |
| **M3** | **Efficiency.** Fewest changeovers, max throughput. | Changeover cost terms exist but half the table is zero | Changeover count/hours reported every run; ≤ the current manual plan's (baseline measured in the trial) |
| **M4** | **Live data, not copies.** | Copied sample files | Built from live links; every number traceable; stale data warns |
| **M5** | **Honest scores.** No 100 without data; maintenance gone. | 3 wrong scores + maintenance phantom | Maintenance removed; other wrong scores fixed; no score of 100 on missing data |
| **M6** | **Daily use.** | No trial | Carsten opens it every workday for a 3-week trial; weekly roll ≤ 15 min |

**Done = M1–M5 green during a 3-week daily trial (M6) and Carsten says go.** Otherwise a written fix list — not more features.

## 5. Constraint inventory — what the solver actually enforces today (audited from code)

Each row is a constraint the user asked to verify. **Verification status: needs the P2 probe pass** (assert each on a real solve output; contracts C1–C6 in `tests/test_solver_contracts.py` already cover some).

| # | Constraint | What it does today | Where (code) | Audit note |
|---|---|---|---|---|
| 1 | **Min run time** | Every production block runs ≥ `min_run_hours`, and ≥ 50% of qty_min worth of time; split segments each meet it | `model_builder.py:258-266` | ✅ **4 h is the rule** (`flowstate.toml:12`), adjustable via config |
| 2 | **Batch size** | Production between qmin and qmax (from `demand_plan.csv` lower/upper %) | `model_builder.py:321-324` | Verify qmin/qmax against AZAP intent |
| 3 | **Capability** | Orders only run on lines marked capable; orders with no capable line are reported short, never placed | `:246, 389, 712, 1021` | ⚠️ 2 demand SKUs (280698, 570560) have no capable line — flagged, never scheduled |
| 4 | **One thing at a time** | NoOverlap per line over production + CIP blocks | `:930-932` | Contract C3 tests it |
| 5 | **CIP can't be pushed past max** | Max interval is HARD; deadline = last clean end + max_cip_hrs (`line_cip_hrs.csv`); duration 6 h | `:691-841` | Verify cip_info ↔ line_cip_hrs agree (they do: 120/144) |
| 6 | **Changeovers** | Each block ≤1 successor/predecessor; cost from `changeovers.csv` setup_hours + attribute columns × weights | `:494-526, 581` | ✅ **Fixed 2026-08-13 (user data + plant knowledge):** 37,830 rows; `ffs_change` alive (12,306 — pouch-size changes 90/110/120 g on Volpak/Bossar); ttp = cooking/mixing (Tetrapak), topload = sleeving, casepacker = casing; all demand SKUs present. Zeros are real (user-confirmed) — cost is attribute-weighted |
| 7 | **Max lines per order** | ≤ 2 lines per order | `:327` | Matches `max_lines_per_order = 2` |
| 8 | **Due dates** | Soft hourly penalty for late (late_weight 200) + hard end cap at due + 1 h | `:158-193` | Verify vs "on time" definition |
| 9 | **Demand total** | Total demand is hard; cross-week movement is a weighted preference | objective in `scenario_runner.py` | Simplifies under the new goal |

**Objective today:** weighted sum — changeover 120, makespan 6, late 200, CIP-defer 5, week-deviation 40. "Minimal changeovers" is already the dominant term; the P2 work makes it the *only* tuning target (one objective, no knobs).

## 6. Data changes

1. `line_rates.csv` → `data/reference/line_rates.csv` (kept in `.hermes/desktop-attachments/` until P2). ⚠️ **Schema discovery (2026-08-12):** the loader's line-rates branch (`data_loader.py:167-189`) expects a **monthly** table (`line_id, Month, rate_kgph`) keyed to the planning month — the flat file has no `Month` column and crashes the loader (`KeyError`). **P2 task: make `Month` optional (absent = flat rate for all months) + regression test.**
2. `flowstate.toml`: `use_sku_rates = false`.
3. Ripple: `data_health.py:337` (the use_sku_rates check flips its advice) and scorecard rate lookups (e.g. `cap_cip_forfeited_kg` average kg/h) use flat rates.
4. Live links (P1): manprg, CIP, stock check, AZAP read from the real sources with refresh + stale warnings + file fallback. **Demand live link = `data/reference/demand_plan_summary.csv` (bridge-refreshed daily) — user decision 2026-08-13; no external path needed.**
5. Changeover data fix (P1/P2): populate `setup_hours`/attribute columns from VIF/plant knowledge; resolve the dead `ffs_change` column; add the 2 missing demand SKUs (280698, 570560).

## 7. Work phases (Claude Code executes each; Director verifies before "done" is said)

- **P0 — Sign-off & setup.** Answer §10 questions; Claude Code verified & logged in (Claude Max, v2.1.229); tidy the tree (commit or revert the loose WW33 files, fix the stale `test_cip_info` expectation); commit the charter.
- **P1 — Live data links.** Connectors for manprg, CIP, stock, AZAP with refresh, stale warnings, file fallback. Exit: Director verifies each link against the real sources.
- **P2 — Audited, executable core.** Probe-verify every constraint in §5 (fix what the audit finds — e.g. min-run rule, ffs_change dead column, missing SKUs); flat rates on; maintenance out of the scorecard; solver always produces a plan that fits. Exit: full suite + probe pass + real-solve gate green.
- **P3 — Honest scorecard.** Fix the remaining wrong scores (CIP cap artifact, Service kg); no 100 without data; raw inputs visible.
- **P4 — Daily screens.** Reconcile (stock flags first-class), bidirectional sandbox, lock/export, track; one-action weekly roll. Not tied to the current UI.
- **P5 — 3-week daily trial** with Carsten, measuring M1–M6.
- **P6 — Go / no-go.**

## 8. What we stop doing

- Solver knob-fiddling and experiment harnesses (champion gate, cron campaigns — archived). One objective, no knobs.
- Per-SKU rate calculations (replaced by flat line rates).
- Scorecard expansion — maintenance is *removed*; only the wrong scores get fixed.
- PDF parsing beyond cross-check; multi-plant work; the 10-page UI → daily screens (sandbox survives).

## 9. Housekeeping before P1

1. Commit or revert the loose WW33 files (`calendar_blocks.csv`, `cip_info.csv`, `scripts/regenerate_scenarios.py`, `data/_scenario_regeneration/`, `data/versions/`) and update the stale `test_cip_info` assertion with them.
2. `feature/handoff` is the trunk; `main` stays frozen.
3. Replace the README's old three-phase vision with this charter.

## 10. Decisions — LOCKED (2026-08-12)

1. **Demand source:** ✅ **DONE — `data/reference/demand_plan_summary.csv` is the live link** (bridge-refreshed daily into the repo); no external path needed.
2. **Time shape:** 2 weeks locked + 1 flexible, rolling weekly. ✅
3. **Screens:** rebuild the daily screens around the sandbox (not tied to the current UI). ✅
4. **Min run time:** 4 h stands (coded), adjustable via config. ✅
5. **Changeover table:** fix the data from VIF/plant knowledge (populate setup_hours/attributes, resolve dead `ffs_change`, add missing SKUs) — part of P1/P2.
